import * as React from 'react';
import { Avatar } from 'react-native-paper';

const AppAvatar = () => (
  <Avatar.Image size={58} style={{margin:30}} source={require('../../assets/nijat.png')} />
);

export default AppAvatar